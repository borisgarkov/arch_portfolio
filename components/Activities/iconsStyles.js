import picturesFolder from "./config";

const imageBox = {
    opacity: 0.7,
    backgroundImage: `url(${picturesFolder}/frame.png)`,
    backgroundSize: 'contain',
    width: '170px',
    height: '170px',
    position: 'relative',
};

export {
    imageBox,
}