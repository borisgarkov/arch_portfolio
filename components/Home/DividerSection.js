import { Divider } from "@mui/material";
import styles from './divider-style.module.css';

export default function DividerSection() {
    return (
        <Divider className={styles.divider} />
    )
};